<div class ="container text-center">
    <div class ="row">
      <div class ="col-md-6 col-md-offset-3">
        <%= simple_form_for([@cocktail, @dose]) do |f| %>
          <%= f.input :description, placeholder: "5cl" %>
          <%= f.association :ingredient %>
          <div class="actions">
            <%= f.button :submit, class: 'btn btn-primary'%>
          </div>
        <% end %>
      </div>
    </div>
  </div>
